# 검증 결과 평가 — episode5_test_draft (2026-07-26)

`data/episode5_test_draft.txt`(실제 5화의 약 28% 분량으로 재구성한 초고, 6개 카테고리에
의도적으로 오류 1개씩 심어둠 — [data/episode5_test_draft_errors.md](../data/episode5_test_draft_errors.md)
참고)를 정합성 리포트 파이프라인으로 검사한 결과를 심어둔 오류와 대조해 정리한다.

## 실행 당시 설정

- `TARGET_CLAIMS_PER_CHUNK = 4`, `MAX_CLAIMS_PER_CHUNK = 5` (청크당 claim 밀도 목표/상한)
- `MIN_TOOL_CALLS = 2`, `MAX_TOOL_CALLS = 4` (claim 검증 시 도구 호출 최소/최대)
- `VERIFY_CONCURRENCY = 5` (claim 검증 동시 실행 수 상한)
- 결과: claim 17개 추출 (모순 2 / 일치 6 / 확인불가 9)

## 오류별 결과

| # | 카테고리 | 심어둔 오류 | 추출됐나? | 판정 | 결과 |
|---|---|---|---|---|---|
| 1 | 생사 | 천인호가 죽었다는 소문이 파다했다(실제로는 살아있음) | ✅ 추출됨 | **unknown** | ❌ 검증 실패 — 모순이어야 함 |
| 2 | 시점/순서 | 유중혁과 "지하철 소동 이후 처음 마주침"(실제로는 2화에서 이미 만남) | ✅ 추출됨 | **contradiction** | ✅ 정탐 |
| 3 | 관계 | 유중혁이 안도하며 순순히 합류(실제로는 혼자 먼저 떠남) | ❌ 미추출 | - | ❌ 추출 실패(미탐) |
| 4 | 소유물 | 이현성이 '부러진 신념'을 소지(실제로는 김독자의 아이템) | ❌ 미추출 | - | ❌ 추출 실패(미탐) |
| 5 | 능력 | 이현성의 전용 스킬 '제4의 벽'(실제로는 김독자 본인의 스킬) | ❌ 미추출 | - | ❌ 추출 실패(미탐) |
| 6 | 장소 | 충무로역 부근 환승 통로(실제로는 금호역→약수역 방향 터널) | ✅ 추출됨 | **unknown** | ❌ 검증 실패 — 모순이어야 함 |

참고: #2와 #3은 원문에서 같은 문장에 붙어 있는데(48행) 앞부분(#2)만 claim으로 뽑히고
뒷부분(#3)은 버려졌다 — 청크당 claim 캡을 낮춘 여파로 보인다.

## 추가로 발견된 오탐(원래 심어둔 6개엔 없던 것)

- **"배후성 '강철의 주인', 체력 12에 근력 9"** claim이 **contradiction**으로 판정됨.
  근거는 "2화 시점엔 체력8·근력8이었는데 지금 다르다"는 것인데, 이 작품은 코인 투자·전투로
  스탯이 계속 오르는 설정이라(3~4화에 실제로 여러 번 상승) 시간이 지나 수치가 달라지는 것
  자체는 정상이다. 그래프에 "이 수치는 시점에 따라 바뀔 수 있다"는 정보가 없어서 스냅샷
  수치 불일치를 그대로 모순으로 오판한 것으로 보인다.

## 수치화

| 지표 | 값 |
|---|---|
| 추출 재현율 (심어둔 6개 중 claim으로 뽑힌 것) | 3/6 = **50%** |
| 검증 재현율 (뽑힌 3개 중 올바르게 모순 판정) | 1/3 = **33%** |
| 전체 end-to-end 재현율 (6개 중 최종 모순 판정까지 간 것) | 1/6 = **16.7%** |
| 모순 판정 정밀도 (모순 2건 중 진짜 오류) | 1/2 = **50%** |
| F1 (전체 재현율·정밀도 기준) | ≈ **0.25** |

## 결론

이전부터 기억해두기로 한 두 가지 문제가 이 테스트에서 그대로 재현됐다.

1. **추출 문제**: 6개 중 3개가 아예 claim으로 뽑히지 않음. 최근 TPM(분당 토큰) 한도 초과에
   대응해 청크당 claim 캡을 5/8 → 4/5로 낮춘 것이 커버리지를 더 낮췄다 — 비용/커버리지
   트레이드오프가 실측으로 확인됨.
2. **검증 문제**: 추출까지 된 3개 중에서도 2개(생사, 장소)가 명백한 모순인데 "unknown"으로
   판정됨.
3. **(신규) 오탐**: 시간에 따라 자연스럽게 변하는 수치(스탯 레벨 등)를 정적 사실로 취급해
   모순으로 오판하는 사례 발견.
