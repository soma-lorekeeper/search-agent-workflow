"""claim 추출·검증에 쓰는 프롬프트 템플릿.

플레이스홀더는 str.format이 아니라 .replace()로 채운다 — background_context(그래프 덤프 +
LLM이 생성한 요약)나 신규 원고 전체에 중괄호가 우연히 섞여 있으면 str.format이 그걸
플레이스홀더로 오인해 KeyError를 내기 때문(옛 archive/src/contradiction_check.py와 동일한
방식).
"""

# OpenAI 호출에 고정으로 넘기는 캐시 키. claim 추출(청크별)·claim 검증(claim별) 호출들이
# 배경 컨텍스트·신규 원고 전체 같은 큰 공통 prefix를 반복해서 보내므로, 캐시 라우팅을
# 안정적으로 묶어주는 게 비용에 큰 영향을 준다(prompt_cache_key가 없어도 내용 매칭으로 어느
# 정도 캐싱되긴 하지만, 병렬로 쏟아지는 요청들이 같은 캐시에 확실히 걸리게 하려면 명시가
# 필요하다 — lorekeeper.pipeline.build_llm()이 추출 LLM에 쓰는 것과 같은 이유). 추출/검증은
# 프롬프트 모양이 완전히 달라서 키를 분리한다 — 같은 키를 쓰면 서로 다른 prefix가 캐시를
# 경합해 적중률이 떨어질 수 있다.
EXTRACTION_CACHE_KEY = "gyeol-contradiction-extract"
VERIFY_CACHE_KEY = "gyeol-contradiction-verify"

# claim 추출(청크별)에 쓰는 시스템 프롬프트. {target_chunk}는 없다 — 청크 본문은 유저
# 메시지로 넘긴다(시스템 프롬프트를 청크마다 동일하게 유지해야 프롬프트 캐싱이 걸린다).
CLAIM_EXTRACTION_PROMPT = """\
너는 웹소설 신규 원고에서, 기존 세계관 설정과 대조해 검증해야 할 "사실 주장(claim)"을
뽑아내는 어시스턴트다.

[지금까지의 세계관 배경]
{background_context}

[신규 회차 원고 전체]
{full_draft}

사용자 메시지로 위 신규 회차 원고의 한 조각(연속된 구간)이 그대로 주어진다. **그 조각
안에서만** claim을 뽑아라 — [신규 회차 원고 전체]는 그 조각을 이해하기 위한 맥락으로만
참고하라(예: 조각 안의 "그가 다시 나타났다"에서 '그'가 누구인지, 이 원고의 앞부분에서
확인). [신규 회차 원고 전체] 자체에서 claim을 뽑으라는 게 아니다.

인물의 생사/소유물/능력/관계/시점·순서/장소 중 하나라도 전제하거나 서술하는 문장을 뽑되,
이 조각에서 대략 {target_claims_per_chunk}개의 claim을 목표로 하라 — 다만 이건 목표치일
뿐이다. 검증할 근거가 없는 구간(순수 묘사·의성어·감탄사)에는 억지로 claim을 만들지 마라.
반대로 사실이 밀집된 구간이면 목표보다 더 뽑아도 된다. 단순 묘사·감정 표현이나 완전히 새로운 사건
자체보다는, [지금까지의 세계관 배경]이나 [신규 회차 원고 전체]의 앞부분에 **이미 확립된
상태를 전제하거나 언급하는 서술**(예: "죽었던 A가 나타났다", "B가 여전히 그 물건을 갖고
있었다")을 우선하라. 배경에도 원고 앞부분에도 전혀 없는 완전히 새로운 사건·인물 묘사는
검증할 근거가 없으니 claim에서 제외하라.

각 claim마다 다음 필드를 채워라:
- quote: **조각 원문**에서 그대로 인용한 문장(또는 핵심 구절)
- entities: 이 claim과 관련된 핵심 고유명사(인물/아이템/장소 등) 목록
- category: 생사 | 소유물 | 능력 | 관계 | 시점/순서 | 장소 | 기타 중 하나

한 조각에서 아무리 많아도 {max_claims_per_chunk}개를 넘기지 마라(그 이상 필요하다고
느껴져도 가장 중요한 것들로 추려라).

JSON 객체로만 답하라: {"claims": [{"quote": "...", "entities": ["..."], "category": "..."}]}
"""

VERIFIER_SYSTEM_PROMPT = """\
너는 웹소설 신규 회차의 서술 하나(claim)가 기존 세계관 설정과 모순되는지 판단하는
어시스턴트다. 필요하면 아래 도구를 스스로 골라 호출해 근거를 모은 뒤 최종 판정한다.

[판정 원칙] (과거 실수를 통해 확정된 규칙이므로 반드시 지켜라)
- claim에는 핵심 사실(누가/무엇을/어떤 상태인지)과, 그 주변을 꾸미는 부가적 묘사(수식어,
  감정, 분위기, 세부 표현)가 섞여 있다. **핵심 사실만** 근거와 대조하라. 부가 묘사가
  근거에 토씨 하나 안 틀리고 등장하지 않는다고 해서 모순으로 판단하지 마라.
- "contradiction"은 근거와 claim이 **논리적으로 양립 불가능할 때만** 쓴다(예: 근거는
  "A가 죽었다"고 하는데 claim은 "A가 살아있다"고 하는 경우). 단순히 근거에 claim의 특정
  표현이 안 보인다는 이유만으로 contradiction을 주지 마라 — 그런 경우는 "unknown"이다.
- 근거가 claim의 핵심 주장을 뒷받침하지도, 반박하지도 못한다면(관련성이 약하거나 다른
  세부사항만 확인된다면) "unknown"으로 판단하라. "unknown"을 남발하는 것보다, 확신 없이
  "contradiction"을 주는 게 더 나쁘다 — 오탐(false positive)은 작가의 신뢰를 잃는다.
- "consistent"는 근거가 claim의 핵심 사실을 지지하거나, 최소한 반박하지 않을 때 쓴다.

[지금까지의 세계관 배경]
{background_context}

[사용 가능한 도구]
{tool_guide}

[도구 호출 방침]
- 이 claim 하나를 검증하는 데 최대 {max_tool_calls}번까지 도구를 호출할 수 있다.
- 근거가 이미 충분하면 더 호출하지 말고 즉시 최종 답을 내라 — 예산을 다 쓸 필요는 없다.
- 첫 시도 결과가 빈약하거나 무관하면, 다른 도구나 다른 검색어로 다시 시도하라(같은 질문을
  그대로 반복하지 마라).

[최종 답변 형식]
더 이상 도구를 부르지 않기로 했으면, 반드시 아래 JSON 객체 하나만 출력하라(다른 텍스트
금지):
{"label": "contradiction|consistent|unknown",
 "established_fact": "판단 근거가 된 기존 설정 요약 한두 문장(없으면 빈 문자열)",
 "source_episode": 근거가 된 화 번호(정수, 모르면 null),
 "explanation": "왜 그렇게 판단했는지 한두 문장"}
"""
