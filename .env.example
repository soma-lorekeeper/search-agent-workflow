# `.env` 로 복사해서 쓴다. `.env` 는 커밋되지 않는다.
#
#   cp .env.example .env
#
# 프로덕션에서는 이 파일을 쓰지 않는다. 배포 스크립트(mvp-infra-iac 의
# deploy-agent-remote.sh)가 SSM/Secrets Manager 에서 값을 읽어
# /opt/agent/agent.env 를 만들고 systemd 가 그걸 주입한다.
# 그래서 프로덕션 자격증명은 어느 레포에도 남지 않는다.
#
# 위쪽은 **채워야 하는 값**, 아래 "선택 설정"은 전부 기본값이 있어 평소엔 건드리지 않는다.

# ---------- OpenAI ----------
OPENAI_API_KEY=sk-...

# **채팅 전용** 모델. 탐지(추출·판정)는 평가 하네스와 같은 모델을 써야 해서
# 아래 EXTRACTION_MODEL 을 쓴다. 채팅은 /v1/responses 를 쓰므로 추론이 기본인 모델
# (gpt-5.6-luna 등)을 넣어도 동작한다.
# (구키 OPENAI_MODEL 도 아직 읽는다 — 프로덕션 배포가 키를 교체할 때까지의 과도기 fallback.)
CHAT_MODEL=gpt-5.6-luna

# 인덱싱(그래프 추출)용 모델. 지정 안 하면 gpt-5.6-luna.
# **탐지(추출·판정)도 이 모델을 쓴다** — 평가 하네스가 이 모델로 검출 22/25 를 쟀고,
# 서비스가 다른 모델로 돌면 그 수치가 근거를 잃는다. 채팅만 위의 CHAT_MODEL 을 쓴다.
# (구키 LOREKEEPER_MODEL 도 아직 읽는다 — CHAT_MODEL 의 fallback 설명과 같은 과도기.)
#
# 추론 강도는 env 로 받지 않는다(하드코딩): 인덱싱 high, 탐지는 미지정(하네스 기본값),
# 채팅 high. 품질 실측이 이 값들 기준이라 설정으로 낮아지면 수치가 조용히 근거를 잃는다.
EXTRACTION_MODEL=gpt-5.6-luna

# ---------- 그래프 DB (Neo4j) ----------
# 로컬은 docker-compose.yml 이 띄우는 컨테이너.
# 프로덕션은 별도 EC2 이고, 주소는 사설 IP 라 외부에서 접근할 수 없다.
#
# ⚠️ API 서버(lorekeeper-backend)의 compose.dev.yml 도 같은 포트(7474/7687)에 Neo4j 를
# 띄운다. 둘을 동시에 올리면 포트가 충돌하니 **한쪽만** 띄우고 다른 쪽은 그것을 가리킨다.
# 그쪽은 아직 5.26 이고 여기는 2026.07 이다 — 우리 인덱싱이 쓰는 인덱스·필터가 2026 계열
# 전제라, 그래프를 실제로 쓰는 이쪽 compose 로 띄우는 편이 안전하다.
# (API 서버는 Neo4j 를 헬스체크로만 읽고 쓰기는 하지 않는다.)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=lorekeeper

# ---------- 관계형 DB (PostgreSQL) ----------
# **API 서버(lorekeeper-backend)와 같은 DB 다.** 원고·작품을 읽고 탐지 결과를 그쪽
# 테이블(detection_jobs / detection_findings)에 직접 쓴다.
#
# Spring 과 **같은 변수·같은 형식**을 쓴다 — 같은 DB 를 보는 두 서비스의 접속 정보를
# 한 벌로 관리하기 위해서다. JDBC → libpq 변환은 코드가 한다
# (src/repository/postgres/client.py).
# libpq DSN 하나로 주는 기존 형식(DATABASE_URL)도 여전히 동작한다 — 아래 선택 설정 참고.
#
# 로컬은 양쪽 레포의 compose 가 같은 포트·계정(lorekeeper/lorekeeper)을 쓰므로,
# 둘 중 어느 쪽 compose 로 띄워도 같은 DB 를 본다.
# 프로덕션은 RDS 이고, 비밀번호는 RDS 가 Secrets Manager 에서 관리·회전한다.
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/lorekeeper
SPRING_DATASOURCE_USERNAME=lorekeeper
SPRING_DATASOURCE_PASSWORD=lorekeeper


# ========== 선택 설정 (전부 기본값이 있다) ==========

# 접속할 데이터베이스. Community 는 표준 DB 가 하나뿐이라 보통 기본값 그대로 둔다.
# NEO4J_DATABASE=neo4j

# Postgres 접속을 libpq DSN 하나로 주는 형식(프로덕션 배포 스크립트가 이 키를 쓴다).
# 위의 SPRING_DATASOURCE_* 와 둘 다 있으면 이쪽이 이긴다.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/lorekeeper

# ---------- 동시 호출 상한 ----------
# 모든 OpenAI 호출이 지나는 관문에서 모델별로 묶는다. **거절이 아니라 대기**라
# 낮게 잡아도 요청이 실패하지 않는다.
#
# 아래 기본값은 실측으로 정했다(2026-08-16, tests/test_llm_smoke.py):
#   gpt-5.6-luna            TPM   200,000 / RPM   500
#   text-embedding-3-small  TPM 1,000,000 / RPM 3,000
#   임베딩 호출 지연         평균 512ms
#
# LLM 은 TPM 이 병목이다. 실측(추출 호출): 8,052 토큰/호출, 지연 20.1 초 →
# N=4 에서 1,303 토큰/초로 충전율(3,333/초)의 39%. 추출만 보면 여유가 있지만
# **같은 세마포어를 판정(27k, 3.4 배 무거움)도 쓴다** — 슬롯 4개가 전부 판정이면 162% 다.
# 세마포어는 개수만 세고 무게는 모르므로 무거운 쪽 기준으로 잡는다.
#
# 임베딩은 RPM 이 병목이다.
#
# 임베딩 상한은 "한도의 몇 %"가 아니라 **충전 속도**로 정했다. RPM 3,000 은 초당 50 개씩
# 차는 연속 충전 버킷이라, 지속 소비율이 50/초를 넘으면 서서히 말라 429 가 난다.
# 실측 처리량: N=16 → 25/초, N=24 → 37/초, N=32 → 72/초(충전율 초과).
# 그래서 24 가 지속 가능한 마지막 구간이다. 남는 13/초는 인덱싱 임베딩 몫이다.
#
# ⚠️ EMBEDDING_MAX_CONCURRENCY 를 바꾸면 탐지 검색의 스레드 수
# (src/service/detect/retrieve_service.py 의 _WORKERS)도 같은 값으로 맞춰야 한다.
# 둘이 직렬로 걸려 있어 한쪽만 올리면 앞에서 줄만 길어진다.
# LLM_MAX_CONCURRENCY=4
# EMBEDDING_MAX_CONCURRENCY=24
#
# 응답 헤더(x-ratelimit-*)를 아직 한 번도 못 본 동안 쓰는 가정값. 첫 응답이 오면
# 실제 값으로 교체된다 — 한도가 아니라 콜드 스타트용 임시값이다.
# LLM_ASSUMED_TPM=200000
# LLM_ASSUMED_RPM=500
