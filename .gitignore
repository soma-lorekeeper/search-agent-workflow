# 비밀키
.env

# 소설 원문 (저작권) — 의존성 목록은 예외
*.txt
!requirements.txt
!requirements-dev.txt

# 예전에 라이브러리로 쓰던 별도 레포의 클론. 코드는 src/ 로 들여왔으므로 더 이상 쓰지
# 않지만, 작업 트리에 남아 있는 사본이 실수로 커밋되지 않도록 무시 목록에 둔다.
lorekeeper-poc/

# 가상환경
.venv/

# 로컬 런타임 데이터
logs/
reports/

# 파이썬 캐시
__pycache__/
*.pyc

# OS
.DS_Store

# IDE
.idea/

# 클로드 로컬 설정 (개인 권한 설정 — 팀 공유 대상 아님)
.claude/settings.local.json

# 평가 하네스 아티팩트 — LLM 응답 캐시·리포트라 수백 MB. 재실행으로 재생성 가능
data/eval_claims/
data/eval_retrieval/
scratchpad_uvicorn.log
