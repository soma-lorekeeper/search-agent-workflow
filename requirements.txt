# lorekeeper-poc(../lorekeeper-poc를 이 폴더 안에 clone한 것)를 라이브러리로 설치.
# 내부 로직(poc/src/*.py)은 건드리지 않고 import만 한다: from lorekeeper import indexing, build_retrievers, build_retrieval_tools
-e ./lorekeeper-poc/poc

# 우리 쪽 오케스트레이션(에이전트 루프, API 서버)
# langgraph: 채팅 에이전트의 루프를 그래프(model 노드 ↔ tools 노드)로 돌린다.
# openai: 모델 호출은 langchain-openai를 거치지 않고 SDK로 직접 한다 — 예산이 떨어지면 tools를
#         빼고, prompt_cache_key를 걸고, parallel_tool_calls를 끄는 통제가 요청 본문을 우리가
#         조립해야 확실해서다(src/chat/agent.py 참고). 그래서 langchain-core·langchain-openai는
#         직접 쓰지 않아 선언에서 뺐다(langchain-core는 langgraph가 전이 의존성으로 끌고 온다).
langgraph
openai
python-dotenv
fastapi
uvicorn

# 연동 점검용. lorekeeper 가 neo4j 드라이버를 이미 끌고 오지만,
# /api/health 가 그 전이 의존성에 기대지 않도록 직접 선언한다.
neo4j
# 관계형 DB 점검. [binary] 는 libpq 를 함께 담은 휠이라 시스템에 libpq 를 깔 필요가 없다.
psycopg[binary]
