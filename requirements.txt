# KG 인덱싱·검색 계층(src/lorekeeper). 예전에는 별도 clone을 editable로 설치했지만
# 이제 이 레포 안의 코드라, 그쪽 pyproject가 선언하던 의존성을 여기서 직접 선언한다.
neo4j>=6.2.0
neo4j-graphrag[openai]>=1.18.0  # OpenAI LLM/임베딩 사용을 위한 openai 의존성 포함(numpy도 base로 포함됨)
langchain-text-splitters>=0.3.0  # RecursiveCharacterTextSplitter 어댑터용
kiwipiepy>=0.15.0  # 한국어 문장 분리(Kiwi 형태소 기반)
kss>=4.0.0  # 한국어 문장 분리(KSS)
python-mecab-ko>=1.3.7  # kss의 mecab 백엔드. 없으면 조용히 수백 배 느려진다
rapidfuzz>=3.14.5  # 엔티티 병합 resolver의 문자열 유사도

# 우리 쪽 오케스트레이션(에이전트 루프, API 서버)
# openai: 모델 호출은 래퍼를 거치지 않고 SDK로 직접 한다 — 예산이 떨어지면 tools를 빼고,
#         prompt_cache_key를 걸고, parallel_tool_calls를 끄는 통제가 요청 본문을 우리가
#         조립해야 확실해서다(src/common/openai_client.py 참고). 그래서 langchain-core·
#         langchain-openai는 직접 쓰지 않아 선언에서 뺐다.
# langgraph: 지금 코드는 쓰지 않는다. 채팅 루프를 그래프로 옮기는 작업이 별도 PR로 다시
#            올라올 예정이라 선언만 남긴다.
langgraph
openai
python-dotenv
fastapi
uvicorn

# 관계형 DB 연동(원고 조회, 탐지 결과 저장, /api/health 점검).
# [binary] 는 libpq 를 함께 담은 휠이라 시스템에 libpq 를 깔 필요가 없다.
psycopg[binary]
